// ─── Re-exported from Zod schemas (single source of truth) ───────────────────
// All types below are inferred from packages/shared/src/schemas.ts.
// Do not define types independently here — edit the schema instead.

export type {
  User,
  Role,
  Category,
  Module,
  Topic,
  Test,
  TestQuestion,
  Enrollment,
  TopicProgress,
  TestAttempt,
  Certificate,
  JobHubProfile,
  HireRequest,
  JobMatch,
  Business,
  BusinessMember,
} from './schemas'
